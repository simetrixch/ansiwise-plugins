# What this package may never name, anywhere under lib/ and test/.
#
# THE TEST FOR A WORD: could a vendor with the same tool and a completely different product still
# use this package with that word in it? A name the tool gives itself passes and stays off this
# list — this package may say zone, record, token and proxied, because those are the API's own
# words, and it may say SPF, DKIM and DMARC, because those are public record grammars every DNS
# holder publishes in, not any product's invention. A name of one product's domains, layout or
# components fails and goes on it.
#
# One word per line. Blank lines and lines beginning with # are ignored; everything else is matched
# case-insensitively and word-anchored, so a longer name that merely contains a word is no
# occurrence of it.
#
# WORD-ANCHORED CUTS BOTH WAYS, and the blocks below are written knowing it. Anything that is not a
# letter or a digit ends a word, so a bare name reports every longer one built on it with a
# separator: `letsencrypt` reports `letsencrypt-prod` and `LETSENCRYPT_EMAIL` alike, which is why
# only the bare word stands here. A longer name built WITHOUT a separator is a different word and
# has to stand in its own right — `digita` does not report `digitacloud`.
#
# THE LIST LIVES HERE, UNDER tool/, AND NOT BESIDE THE CHECK THAT READS IT. The scan reads test/ to
# the byte, so a list written into test/checks/tool_purity_test.dart would itself be an occurrence
# of every word on it and the check could never pass. That tool/ stays out of the scan is asserted
# by a counter-probe of its own.

# The product built on this package, the platform it stands on, and the organisation behind both.
# This block stands on every list in this tree, word for word.
hostyour
digita
digitacloud
digitaplatform
simetrix
simetrixch

# That product's choices: the distribution it installs, the components it deploys, the tools it
# drives those components with, the vendors it rents from, and the authority it gets its
# certificates from. Each is a fact of one product, not of the DNS API. This block stands on every
# list in this tree too, except that a package never carries its OWN tool — Cloudflare is this
# package's, which is why that word is the one of this kind missing here.
microk8s
argocd
tekton
zot
external-secrets
vault
helm
kubectl
snap
netplan
authentik
hetzner
letsencrypt

# That product's own domain words, and the names of things it happens to publish records for. A DNS
# plugin knows names and records; which installation's names exist is one product's business, and a
# step that named one would carry it into every installation that ever met this package.
tenant
consumer
trunk
git
postfix
headlamp
manager
grafana
