include: ../analysis_options.yaml

linter:
  rules:
    # A test's classes are read next to the test that uses them, so a doc comment on each would
    # repeat what the test name already says. Everything else stays as strict as the library.
    public_member_api_docs: false
