import 'package:ansiwise_checks/audits.dart';
import 'package:ansiwise_vault_kubernetes/ansiwise_vault_kubernetes.dart';

/// declared-answers — the probe plants what an installation plants, for [vaultKubernetesRegistry].
///
/// The one step of this package takes what it needs as an ARGUMENT, out of the program row that
/// names it, and the single axis a product may run the same layout along more than once is filled
/// into those values from a name the row supplies — so the audit states a count of zero and opens no
/// installation tree. The day a step here reads an answer by a name of its own, the same audit holds
/// that name against what the programs declare for it.
Future<void> main() => auditDeclaredAnswers(vaultKubernetesRegistry);
